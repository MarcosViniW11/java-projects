spring:
  application:
    name: projetoTreino
  datasource:
    url: jdbc:mysql://localhost:3306/projetotreinotest?useSSL=false&serverTimezone=America/Sao_Paulo
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080

